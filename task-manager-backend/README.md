# Task Manager API (Django + DRF + JWT)

A secure and powerful Task Management REST API built using Django, Django REST Framework, and JWT Authentication.

## Features

- JWT Authentication (Login/Register)
- Create, Read, Update, Delete (CRUD) Tasks
- Tasks are private to each user
- Task priority, deadline, and status tracking
